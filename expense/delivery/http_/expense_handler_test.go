package http__test

import (
	"testing"
)

func TestCreateExpense(t *testing.T) {
	// t.Run("ce_1:the request body failed because the required field is missing.", func(t *testing.T) {
	// 	body := `{
	// 		"amount": 79,
	// 		"note": "night market promotion discount 10 bath",
	// 		"tags": ["food", "beverage"]
	// 	}`

	// 	req, err := http.NewRequestWithContext(context.TODO(), http.MethodPost, "/article/"+strconv.Itoa(num), strings.NewReader(""))

	// })
}
